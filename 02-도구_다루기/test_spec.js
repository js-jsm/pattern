describe("test", () => {

  it("sqrt should be square root", () => {
    expect(sqrt(4)).toEqual(2);
    expect(sqrt(9)).toEqual(3);
  });

  it("pow should be pow", () => {
    expect(pow(4)).toEqual(16);
    expect(pow(3)).toEqual(9);
  });

});
